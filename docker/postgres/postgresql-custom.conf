vectors.dimensions = 384  # Match your embedding model dimension
vectors.probes = 10      # Number of probes for index scanning
vectors.lists = 100      # Number of lists for IVFFlat index

# Enable required extensions
shared_preload_libraries = 'vector'